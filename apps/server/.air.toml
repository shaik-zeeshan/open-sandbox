root = "."
tmp_dir = "tmp"

[build]
cmd = "go build -o ./tmp/open-sandbox ."
entrypoint = ["./tmp/open-sandbox"]
exclude_dir = ["tmp"]
include_ext = ["go"]
stop_on_error = true
send_interrupt = true
kill_delay = "500ms"
